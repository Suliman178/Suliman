import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { post } from '../lib/apiClient';
import { Button } from '../components/ui/Button';
import type { User } from '../types/user';
export function LoginPage({ onAuth }: { onAuth: (u: User)=>void }) { const [mode,setMode]=useState<'login'|'register'>('login'); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [name,setName]=useState(''); const [error,setError]=useState(''); const navigate=useNavigate(); async function submit(){ setError(''); try { const data = await post<{user:User}>(`/api/auth/${mode}`, { email, password, name }); onAuth(data.user); navigate('/dashboard'); } catch(e:any){ setError(e.message); } } return <div className="flex min-h-[calc(100vh-3.5rem)] items-center justify-center bg-gray-50 p-4"><div className="w-full max-w-md rounded-2xl border border-gray-200 bg-white p-8 shadow-sm"><h1 className="text-2xl font-bold">{mode==='login'?'Sign in':'Create account'}</h1><p className="mt-2 text-sm text-gray-600">Use secure email/password auth for this MVP.</p>{mode==='register'&&<input className="mt-6 w-full rounded-lg border p-3" placeholder="Name" value={name} onChange={(e)=>setName(e.target.value)}/>}<input className="mt-4 w-full rounded-lg border p-3" placeholder="Email" value={email} onChange={(e)=>setEmail(e.target.value)}/><input className="mt-4 w-full rounded-lg border p-3" type="password" placeholder="Password (6+ chars)" value={password} onChange={(e)=>setPassword(e.target.value)}/>{error&&<div className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</div>}<Button className="mt-5 w-full" onClick={submit}>{mode==='login'?'Sign in':'Register'}</Button><button className="mt-4 text-sm font-semibold text-accent-700" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Need an account? Register':'Already have an account? Sign in'}</button></div></div>; }
