import type { ProjectFile } from '../../types/project';
export function FileTabs({ tabs, selectedPath, onSelect, onClose }: { tabs: ProjectFile[]; selectedPath?: string; onSelect: (path:string)=>void; onClose: (path:string)=>void }) { return <div className="flex gap-1 overflow-x-auto border-b border-gray-200 bg-white px-2 pt-2">{tabs.map((f)=><button key={f.path} onClick={()=>onSelect(f.path)} className={`flex items-center gap-2 rounded-t-lg px-3 py-2 text-xs ${selectedPath===f.path?'bg-gray-100 font-semibold':'hover:bg-gray-50'}`}>{f.path.split('/').pop()}<span onClick={(e)=>{e.stopPropagation(); onClose(f.path);}} className="text-gray-400 hover:text-red-600">×</span></button>)}</div>; }
